"""Career Agent OS domain and application boundaries.

The package is introduced slice-by-slice.  It must not become a second set of
ORM models or a wrapper around every legacy service: only behavior migrated to
an approved Career Agent OS Contract belongs here.
"""
