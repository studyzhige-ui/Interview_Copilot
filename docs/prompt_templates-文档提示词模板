# 三份面试文档的 Prompt 模板

> 你的项目已有三份文档（`PROJECT_DOCUMENTATION.md`、`INTERVIEW_QA.md`、`interview_copilot_interview_guide_zh.md`），质量都不错。下面的 Prompt 是用于**全面重写/升级**这三份文档的完整模板。

---

## 使用方式

1. **直接在 Codex / Claude 对话中使用** — 把 Prompt 完整发给 AI，让它扫描你的项目目录并生成文档。
2. **适配你的环境** — Prompt 中标注了 `{{变量}}`，替换成你的实际路径/内容即可。
3. **建议分三轮对话分别生成**，不要一次生成三份，避免上下文过载导致质量下降。

---

## Prompt 1：项目完整技术文档

### 目标

生成一份 **任何人只读这一份文档，就能完整理解项目的架构、数据流、每个模块的实现细节、设计亮点和工程取舍** 的文档。

### Prompt（直接复制使用）

```
你是一名资深后端架构师，擅长撰写高质量技术文档。请为我的项目生成一份完整的项目技术文档。

## 项目根目录
{{d:\Projects\Python\Interview_Copilot}}

## 任务要求

请你：
1. 扫描整个项目目录，阅读所有 Python 源码、配置文件、测试文件、docker-compose、前端入口和评测脚本。
2. 生成一份 Markdown 格式的项目技术文档，保存到 `docs/PROJECT_DOCUMENTATION.md`。

## 文档必须覆盖的内容（缺一不可）

### 第一部分：项目定位与价值
- 项目解决的核心问题（不少于 4 点）
- 与简单 ChatBot 的本质区别
- 面向的用户场景

### 第二部分：总体架构
- 用 Mermaid 画完整架构图（API 层 → 工作流层 → 服务层 → 存储层），每个节点标注文件名和核心职责
- 文字描述各层之间的数据流

### 第三部分：目录结构
- 逐目录、逐文件说明职责（至少到第二层）

### 第四部分：API 层详解
- 每个路由文件的所有端点，包含：HTTP 方法、路径、功能描述、请求/响应关键字段
- 认证机制的完整流程

### 第五部分：核心链路详解（这是最重要的部分，必须极其详细）
对以下每条链路，都要用 **Mermaid 序列图** + **分步骤文字解释** 的方式描述：

1. **普通多轮 RAG Chat Pipeline**
   - 完整执行阶段（从用户消息到流式返回到 post-turn maintenance）
   - Query Planner 的输入、输出、每个字段的含义和设计理由
   - 为什么区分 dense_query 和 sparse_query
   - 并发召回的实现方式
   - direct chat 和 RAG chat 的选择逻辑

2. **上下文 Pipeline**
   - ContextBundle 的每个字段含义
   - TokenBudgeter 的预算分配策略和具体数值
   - PromptRenderer 的区块顺序和设计理由
   - sanitize → truncate → repair → assemble 每一步做什么
   - Working State 和 Interview State 的区别和各自更新机制

3. **长期记忆系统**
   - MemoryItem 的完整字段和每个字段的作用
   - 记忆抽取流程（触发时机、LLM 抽取、置信度过滤、合并逻辑、normalized_key 的作用）
   - 记忆向量索引（独立 collection 的设计理由、embedding 模型和参数）
   - 混合召回（向量 + 词法 + HybridRetriever 融合打分公式）
   - 召回后的副作用（recall_count、staleness_note 等）
   - 写入失败的降级策略

4. **RAG 摄取与检索**
   - 自适应切块的完整策略（哪种内容用哪种 parser，为什么）
   - 检索链路的每一步（dense → BM25 → RRF fusion → reranker → score gate）
   - 多租户隔离的实现（dense 和 BM25 两侧分别怎么过滤）
   - 防幻觉机制（score gate + lexical fallback + SYSTEM_EMPTY_WARNING）
   - Reranker 的选型理由和参数

5. **ReAct 工具代理**
   - 与普通 Chat 链路的隔离点
   - Function Calling 的执行循环
   - 工具注册、Pydantic 参数校验、schema 导出
   - AgentBudget 的所有熔断维度和触发后的行为
   - Agent Trace 的数据结构和用途
   - 记忆读写的权限控制

6. **面试音频分析**
   - 上传方式（presigned URL vs 后端直传）
   - Celery 任务的状态机（PENDING → TRANSCRIBING → ANALYZING → COMPLETED/FAILED）
   - WhisperX + diarization 的输出格式和为什么选 Markdown
   - QA pair 抽取逻辑（speaker 解析、面试官/候选人角色判定、chunk 分块策略）
   - 长转录的分块处理和汇总

### 第六部分：数据模型
- 每个 ORM model 的关键字段和关联关系

### 第七部分：存储设计
- PostgreSQL / Redis / Milvus / MinIO 各自的用途和关键参数
- Milvus HNSW 参数和选择理由

### 第八部分：配置体系
- 所有配置项分类列出，标注默认值和作用

### 第九部分：测试与评测
- 测试覆盖范围和运行方式
- RAG 评测指标（Hit Rate、MRR、Precision、Recall、nDCG、RAGAS）
- Agent 轨迹评测指标

### 第十部分：部署
- 本地开发的完整启动步骤
- docker-compose 的服务拓扑

### 第十一部分：关键设计取舍
- 至少 5 个明确的工程取舍决策，每个包括：选择了什么、放弃了什么、理由是什么

### 第十二部分：推荐阅读顺序
- 按照理解难度，给出源码阅读路径

## 格式要求
- 中文撰写
- 用中英双语标注核心概念（例如"检索增强生成（Retrieval-Augmented Generation, RAG）"）
- 第一次出现的技术术语必须中英双语标注，后续可以只用缩写
- 所有架构关系用 Mermaid 图表
- 所有执行流程用 Mermaid 序列图
- 代码片段只在说明数据结构和关键参数时使用，不要大段粘贴源码
- 文档长度不限，以覆盖完整为最高优先级
```

---

## Prompt 2：面试 QA 文档（双 Agent 模式）

### 目标

用 **Interviewer Agent（面试官）** 和 **Candidate Agent（候选人）** 两个角色交互生成 Q&A，覆盖所有可能的面试追问。

### Prompt（直接复制使用）

```
你现在同时扮演两个角色来完成一份面试 QA 文档的撰写。

## 角色定义

**Interviewer Agent（面试官 Agent）**：
- 你是一名有 10 年经验的大厂后端面试官
- 你擅长从架构设计、技术选型、实现细节、性能优化、故障处理、工程取舍等多个维度追问
- 你的目标是找到候选人对项目理解的盲区
- 你会问以下几类问题：
  1. **概览类**：项目是什么、解决什么问题、和竞品/替代方案的区别
  2. **架构设计类**：为什么这样分层、为什么这样分链路、数据怎么流转
  3. **实现细节类**：某个具体功能是怎么实现的、代码层面怎么做的
  4. **技术选型对比类**：为什么用 X 不用 Y？（必须覆盖所有技术栈组件）
  5. **参数与配置追问类**：某个参数为什么设成这个值？改了会怎样？
  6. **故障与边界类**：如果 XX 失败了怎么办？如果数据量变大怎么办？
  7. **优化方向类**：你觉得还能怎么优化？
  8. **质疑类**：这不就是套壳吗？这个设计有什么问题？

**Candidate Agent（候选人 Agent）**：
- 你就是这个项目的开发者
- 你要基于实际代码实现回答，不能编造不存在的功能
- 回答要有层次：先给结论，再说实现，最后说理由或取舍
- 回答中要体现工程思维，而不是只堆技术名词
- 对于"为什么不用 XX"类问题，必须说清楚 XX 的优劣和为什么在当前场景不选

## 项目根目录
{{d:\Projects\Python\Interview_Copilot}}

## 任务要求

1. 扫描整个项目，阅读所有源码和配置。
2. 由 Interviewer Agent 提出问题，Candidate Agent 回答。
3. 生成的文档保存到 `docs/INTERVIEW_QA.md`。
4. 问题总数不少于 120 个。

## 必须覆盖的问答主题（每个主题至少 5-8 个问题）

### 1. 业务与产品定位（8-10 题）
- 项目解决什么问题
- 与普通 ChatBot 的区别
- 核心业务链路
- 用户价值
- 项目最大的工程难点

### 2. 整体架构（8-10 题）
- 分层方式和理由
- 双链路设计的动机
- 为什么普通问答不走 Agent
- 各存储组件的职责

### 3. API 与鉴权（6-8 题）
- JWT 认证流程
- WebSocket vs SSE 选择
- 接口设计原则

### 4. 普通多轮问答 Pipeline（10-12 题）
- 完整执行流程
- Query Planner 每个字段的设计理由
- dense_query vs sparse_query 为什么分开
- 并发召回的实现
- direct chat vs RAG chat 的切换
- post-turn maintenance 为什么异步

### 5. 上下文 Pipeline 与状态管理（10-12 题）
- Raw Transcript 的角色
- ContextBundle 设计
- 为什么不直接拼字符串
- PromptRenderer 区块顺序
- Working State vs Interview State
- Compaction 触发条件和策略
- TokenBudgeter 的具体预算

### 6. 长期记忆系统（10-12 题）
- 记忆类型和筛选标准
- 抽取流程和 confidence 阈值为什么是 0.65
- normalized_key 的作用
- 为什么用独立 Milvus collection
- HybridRetriever 打分公式中每项权重的理由
- 召回后的副作用
- 写入失败的降级

### 7. RAG 摄取与检索（10-12 题）
- 自适应切块的选择逻辑
- 为什么不用纯向量检索
- BM25 为什么在应用层构建
- Reranker 为什么选 BGE
- RAG_MIN_SCORE 为什么是 0.5
- 防幻觉的完整策略
- 多租户隔离怎么做

### 8. ReAct Agent（8-10 题）
- 工具清单和设计
- Pydantic schema 校验的价值
- AgentBudget 每个维度的设计理由
- Agent Trace 的数据结构
- 记忆读写权限

### 9. 音频分析与异步任务（8-10 题）
- 为什么用 Celery 不用 BackgroundTasks
- WhisperX vs Whisper
- 说话人分离的作用
- QA pair 抽取逻辑
- chunk 分块策略

### 10. 技术选型横向对比（10-12 题，这部分极其重要）
每一组对比必须包含：选了什么、放弃了什么、两者各自的优劣、为什么在当前场景选这个

- FastAPI vs Flask vs Django
- LlamaIndex vs LangChain
- Milvus vs FAISS vs Chroma vs pgvector
- Celery vs FastAPI BackgroundTasks vs Dramatiq vs RQ
- WhisperX vs Whisper vs Google Speech-to-Text
- PostgreSQL vs MongoDB（为什么用关系型）
- Redis 在项目中的角色（为什么不用 RabbitMQ 做 broker）
- MinIO vs 直接本地文件系统
- DeepSeek vs GPT-4 vs Claude（模型选型）
- BM25 应用层 vs Elasticsearch vs 独立 sparse index
- HNSW vs IVF_FLAT vs DiskANN

### 11. 参数设置追问（8-10 题）
- RAG_MIN_SCORE = 0.5 为什么是这个值
- MIN_CONFIDENCE = 0.65 怎么来的
- TokenBudgeter 的 4000/1600/5000 分配理由
- HybridRetriever 权重 0.6/0.35/0.15/0.05 的理由
- HNSW M=16, efConstruction=200, efSearch=64 为什么这样设
- AGENT_MAX_STEPS 默认值的考量
- Compaction 5000 tokens 阈值怎么定的

### 12. 安全与隔离（5-8 题）
- 数据隔离的层次
- prompt injection 的防护
- 工具调用越权防护
- 生产化安全清单

### 13. 评测与测试（5-8 题）
- 测试覆盖
- RAG 评测指标和方法
- Agent 轨迹评测
- 为什么 RAG 和 Agent 分开评测

### 14. 优化方向与未来规划（5-8 题）
- RAG 优化方向
- 记忆优化方向
- Agent 优化方向
- 上下文 pipeline 优化方向

### 15. 总结与反思类（5-8 题）
- 项目最亮的点
- 最大收获
- 如何应对"套壳"质疑
- 如果重做会先改什么
- 一句话概括项目

## 格式要求
- 中文撰写
- 每个问题用 `### Q{n}：问题` 格式
- 每个回答用 `A：` 开头
- 回答中的技术术语首次出现时中英双语标注
- 问题按主题分大章节
- 对于技术选型对比类问题，回答必须有对比结构（不能只说选了什么好，要说清放弃的为什么不选）
```

---

## Prompt 3：简历导向面试讲解文档

### 目标

基于已有的项目简历描述，生成一份 **围绕简历每一个 bullet point 展开的面试讲解稿**，确保面试时能把简历上的每一点讲清楚、讲深入、扛住追问。

### Prompt（直接复制使用）

```
你是一名面试辅导专家，帮助候选人准备项目讲解。请根据我的项目简历和实际代码实现，生成一份面试讲解文档。

## 项目根目录
{{d:\Projects\Python\Interview_Copilot}}

## 我的项目简历描述
{{
在这里粘贴你简历上关于 Interview Copilot 项目的完整描述，
包括项目名称、技术栈、所有 bullet points。
例如：

Interview Copilot — AI 面试准备后端系统
技术栈：FastAPI, PostgreSQL, Redis, Celery, Milvus, LlamaIndex, DeepSeek/OpenAI LLM, WhisperX, Vue 3

• 设计并实现了双链路架构：普通问答走确定性 RAG Pipeline，复杂任务走 ReAct Agent，兼顾低延迟和复杂推理
• 构建了多层级上下文管理系统，通过 ContextBundle + PromptRenderer 实现结构化提示词渲染，替代简单的历史拼接
• 实现了混合 RAG 检索：Milvus 向量检索 + BM25 词法检索 + RRF 融合 + BGE Reranker 重排 + 分数阈值防幻觉拦截
• 设计了长期记忆系统，支持跨会话记忆抽取、归一化合并、独立向量索引和混合召回
• 使用 Celery + WhisperX 实现异步面试录音转写，支持说话人分离和自动问答对抽取与评分
• 实现了多租户数据隔离，RAG 知识库、长期记忆和 Agent Trace 均按 user_id 强制过滤
• 构建了 ReAct Agent 治理体系，包含 Pydantic 参数校验、多维预算熔断和全量轨迹记录
• 后端测试覆盖 60+ 用例，并建立了独立的 RAG 和 Agent 评测体系
}}

## 任务要求

请你：
1. 扫描项目代码，验证简历描述的真实性。
2. 对简历的每一个 bullet point，生成以下内容：

### 对每个 bullet point 生成的内容

#### a) 30 秒讲解版
- 用通俗语言讲清楚这一点做了什么，适合面试开场时快速介绍

#### b) 2 分钟深入版
- 展开讲实现细节，适合面试官追问"具体怎么做的"

#### c) 面试官可能的追问（至少 5 个）
- 列出面试官最可能从这一点延伸出的追问
- 每个追问附带参考回答

#### d) 关键代码/配置定位
- 指出这个功能点对应的核心文件和关键函数/类名
- 标注关键参数的默认值和设计理由

#### e) 技术选型辩护
- 如果 bullet point 涉及技术选型，说明为什么选这个、常见替代方案各自的优劣

#### f) 常见翻车点
- 列出讲解这一点时容易犯的错误（比如说得太笼统、漏掉关键细节、搞混概念等）

3. 在文档末尾增加一个章节：

### 简历整体讲解策略
- 推荐的讲解顺序（不是按 bullet point 原始顺序，而是按面试时最有效的逻辑顺序）
- 开场 2 分钟话术
- 如何引导面试官追问你擅长的部分
- 如何应对"这就是调 API"/"这是套壳项目"等质疑
- 如何在追问中自然地展示工程能力而不是只堆技术名词

## 输出格式
- 中文撰写
- 保存到 `docs/RESUME_INTERVIEW_GUIDE.md`
- 每个 bullet point 用二级标题
- 追问用缩进列表
- 代码定位用代码块
- 关键术语中英双语标注
```

---

## 补充建议

### 关于执行顺序

| 顺序 | 文档 | 原因 |
|------|------|------|
| 1 | 项目完整技术文档 | 这是基础，QA 和简历讲解都依赖对项目的完整理解 |
| 2 | 面试 QA 文档 | 基于已生成的项目文档，覆盖所有追问 |
| 3 | 简历讲解文档 | 最后生成，可以引用前两份文档的内容 |

### 关于在 Codex vs Claude 中使用

| 特点 | Codex（代码模式） | Claude（长对话） |
|------|---------------------|-------------------|
| **扫描代码** | ✅ 原生支持，直接读项目目录 | ❌ 需要手动粘贴关键代码或上传文件 |
| **长文档生成** | ✅ 可以直接写入文件 | ✅ 长上下文更强，一次性输出更完整 |
| **推荐用法** | 直接在 Codex 中用 Prompt 1 和 Prompt 2 | 把代码关键部分粘贴进 Claude 后用 Prompt 3 |

> [!TIP]
> **在 Codex 中使用时**，可以直接发 Prompt 内容（不需要 `{{}}` 占位符，因为 Codex 自己会扫描项目目录）。
> 
> **在 Claude 中使用时**，需要在 Prompt 之前先粘贴关键源码文件内容，或通过 Projects 功能上传代码文件，否则 Claude 无法了解你的实际实现。

### 关于你现有文档的评估

你现有的三份文档已经有不错的基础：

| 文档 | 现状 | 可以改进的方向 |
|------|------|----------------|
| `PROJECT_DOCUMENTATION.md` | 覆盖了大部分模块 | 缺少前端细节、缺少各链路的错误处理/降级策略说明、Mermaid 图可以更细致 |
| `INTERVIEW_QA.md` | 90 题，覆盖较全 | 缺少参数追问、技术选型对比不够深入（只说了选了什么好，没充分说放弃的为什么不选） |
| `interview_copilot_interview_guide_zh.md` | 讲解稿格式好 | 没有和简历 bullet point 一一对应，缺少追问和翻车点提醒 |

> [!IMPORTANT]
> 如果你想直接让我（当前的 Codex 会话）帮你生成这三份文档而不是给你 Prompt 模板，请直接告诉我，我可以立即开始扫描代码并生成。
