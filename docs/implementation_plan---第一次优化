# Interview Copilot 项目全面审计与修改计划

经过对项目的后端（FastAPI + Celery）、前端（Vue 3）、RAG 管线、Agent 系统、基础设施的完整审查，以及对行业最佳实践的对比调研，以下是我识别出的 **8 大问题领域** 和详细修改计划。

---

## 问题总览

| # | 领域 | 严重等级 | 核心问题 |
|---|------|---------|---------|
| 1 | 🔴 安全与配置 | P0 | CORS 全放行、密钥硬编码默认值、JWT 7 天过期无 Refresh |
| 2 | 🔴 数据库与并发 | P0 | 同步 DB Session 在 async 路径中泄漏、无连接池调优 |
| 3 | 🟠 架构耦合 | P1 | 双 Agent 管线（`agent_executor` vs `react_agent`）共存导致路径混乱 |
| 4 | 🟠 RAG 管线性能 | P1 | BM25 每次请求全量加载 Docstore、Milvus 连接无复用 |
| 5 | 🟠 事件循环阻塞 | P1 | Celery `asyncio.run()` + 同步 DB 调用在 async 端点内 |
| 6 | 🟡 前端工程 | P2 | 单文件巨型组件（483 行 App.vue）、无路由/状态管理 |
| 7 | 🟡 测试与可观测性 | P2 | 无集成测试、无端到端 RAG 精度回归、Telemetry 空壳 |
| 8 | 🟡 DevOps 与部署 | P2 | 无 Dockerfile、无 CI/CD、生产环境仅 `uvicorn --reload` |

---

## 1. 🔴 安全与配置（P0）

### 1.1 CORS 全放行

**现状**：[main.py:85-91](file:///d:/Projects/Python/Interview_Copilot/backend/app/main.py#L85-L91)
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,  # 与 allow_origins=["*"] 矛盾！
    allow_methods=["*"],
    allow_headers=["*"],
)
```

> [!CAUTION]
> `allow_origins=["*"]` + `allow_credentials=True` 是一个已知的浏览器安全违规组合。浏览器实际上会拒绝发送 cookie，但这暴露了 API 对 CSRF 攻击的脆弱性。

**修改方案**：
- 从 `.env` 读取 `CORS_ORIGINS`（逗号分隔），开发默认 `http://localhost:5173`
- 在生产部署中严格限制为实际域名

### 1.2 JWT Token 缺陷

**现状**：[security.py:29-37](file:///d:/Projects/Python/Interview_Copilot/backend/app/core/security.py#L29-L37)
- Token 有效期 **7 天**，无 Refresh Token 机制
- 使用 `datetime.utcnow()` (Python 3.12 已弃用)
- 默认 `SECRET_KEY = "change-me-for-local-development"`，生产容易遗忘修改

**修改方案**：
- 实现 Access/Refresh Token 双 Token 机制（Access 30 min / Refresh 7 天）
- 迁移到 `datetime.now(timezone.utc)`
- 启动时检测 SECRET_KEY 是否为默认值，非开发环境报错中止

### 1.3 Settings 类不使用 Pydantic BaseSettings

**现状**：[config.py](file:///d:/Projects/Python/Interview_Copilot/backend/app/core/config.py)  
`Settings` 类是普通 Python class，手动 `os.getenv()` 100+ 次。**没有类型验证、没有环境变量缺失警告**。

**修改方案**：
```python
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    DATABASE_URL: str = "postgresql://..."
    SECRET_KEY: str  # 必须提供，否则启动失败
    model_config = SettingsConfigDict(env_file=".env", extra="ignore")
```

---

## 2. 🔴 数据库与并发（P0）

### 2.1 同步 Session 在 async 路由中使用

**现状**：多个 async 端点通过 `Depends(get_db)` 获取 **同步** `Session`

```python
# database.py — 同步引擎
engine = create_engine(settings.DATABASE_URL)
SessionLocal = sessionmaker(bind=engine)

# chat.py:242 — async WebSocket 中使用同步 session
async def websocket_chat_endpoint(..., db: Session = Depends(get_db)):
    session_row = db.query(ChatSession)...  # 阻塞事件循环！
```

> [!WARNING]
> 在 `async def` 中直接调用同步 `db.query()` 会阻塞整个 uvicorn 事件循环。所有并发 WebSocket 连接会被一个慢查询卡住。

**修改方案**：
- 引入 `AsyncEngine` + `async_sessionmaker` (SQLAlchemy 2.x 原生支持)
- 或至少对 WebSocket 端点使用 `run_in_threadpool`
- 添加连接池配置：`pool_size`, `max_overflow`, `pool_recycle`

### 2.2 手动 `SessionLocal()` 散布各处

**现状**：`memory_extraction_service.py`、`transcript_service.py` 等 **11 处**直接 `db = SessionLocal()` 创建 session，不走 FastAPI 依赖注入。

**风险**：
- session 泄漏（try/finally 不覆盖所有异常路径）
- 无法在测试中注入 mock session
- 无事务边界控制

**修改方案**：
- 统一使用依赖注入或 context manager
- 服务层 class 通过构造器接收 `session_factory`

---

## 3. 🟠 架构耦合（P1）

### 3.1 双 Agent 管线共存

项目中存在 **两套完全独立的** Agent 执行引擎：

| 路径 | 文件 | 使用方式 |
|------|------|---------|
| 直接流式 | [agent/agent_executor.py](file:///d:/Projects/Python/Interview_Copilot/backend/app/agent/agent_executor.py) | WebSocket/SSE，LlamaIndex `astream_complete` |
| ReAct Agent | [agent_runtime/react_agent.py](file:///d:/Projects/Python/Interview_Copilot/backend/app/agent_runtime/react_agent.py) | Agent API，OpenAI Function Calling |

**问题**：
- `agent_executor.py` 用 LlamaIndex 的 `astream_complete`（无 tool 调用）
- `react_agent.py` 用原生 OpenAI SDK 的 Function Calling（有 tool 调用）
- 两者共享部分基础设施（planner, context_service, memory），但 **重复实现** 了 context 组装
- WebSocket/SSE 对话实际走的是 `agent_executor`，**不经过任何 tool 调用**

**修改方案**：
- 统一为单一 Agent 管线，以 `react_agent.py` 的 Function Calling 为基础
- 为 WebSocket/SSE 添加 streaming wrapper（将 ReAct Agent 的最终回答 stream 给客户端）
- 删除或标记 `agent_executor.py` 为 deprecated

### 3.2 Query Planner 与 Router 并存

- [agent/planner.py](file:///d:/Projects/Python/Interview_Copilot/backend/app/agent/planner.py) — LLM-based query planning
- [agent/router.py](file:///d:/Projects/Python/Interview_Copilot/backend/app/agent/router.py) — 似乎是另一套路由逻辑

**修改方案**：保留 `planner.py`，将 `router.py` 逻辑合并或移除。

---

## 4. 🟠 RAG 管线性能（P1）

### 4.1 BM25 全量加载 Docstore

**现状**：[retriever.py:248-265](file:///d:/Projects/Python/Interview_Copilot/backend/app/rag/retriever.py#L248-L265)

```python
docstore = PostgresDocumentStore.from_uri(uri=settings.DATABASE_URL)
all_nodes = list(docstore.docs.values())  # 全量加载到内存！
filtered_nodes = [n for n in all_nodes if _metadata_matches_scope(...)]
bm25_retriever = BM25Retriever.from_defaults(nodes=filtered_nodes, ...)
```

> [!WARNING]
> **每次检索请求**都从 PostgreSQL 全量读取所有文档节点到内存，然后 Python 层面过滤。当文档量增长到 10K+ 节点时，延迟将变得不可接受。

**修改方案**：
- 实现 BM25 索引预构建和缓存（per-user 索引，按需惰性创建）
- 或迁移到数据库原生全文搜索（PostgreSQL `tsvector`/`tsquery` + GIN 索引）
- 短期：添加 user-scoped docstore 查询，避免全量加载

### 4.2 Milvus 连接不复用

**现状**：`query_knowledge_base()` 和 `ingest_document()` 每次调用都 `new MilvusVectorStore()`

**修改方案**：
- 使用模块级单例或连接池
- 通过 LRU 缓存复用 VectorStoreIndex 实例

### 4.3 Reranker 热加载阻塞

**现状**：[retriever.py:32](file:///d:/Projects/Python/Interview_Copilot/backend/app/rag/retriever.py#L32) — `init_rag_settings()` 在模块导入时执行，会阻塞 uvicorn 启动

**修改方案**：延迟初始化到 lifespan，已有部分实现但不彻底

### 4.4 EMBEDDING_DIM 硬编码

**现状**：`EMBEDDING_DIM = 512` 在 [retriever.py:38](file:///d:/Projects/Python/Interview_Copilot/backend/app/rag/retriever.py#L38) 和 [ingestion.py:20](file:///d:/Projects/Python/Interview_Copilot/backend/app/rag/ingestion.py#L20) 各写了一遍

**修改方案**：移到 `config.py` 统一管理，或从模型元数据自动推断

---

## 5. 🟠 事件循环与异步（P1）

### 5.1 Celery 任务中 `asyncio.run()`

**现状**：[worker/tasks.py:13-15](file:///d:/Projects/Python/Interview_Copilot/backend/app/worker/tasks.py#L13-L15)
```python
def run_async(coro):
    return asyncio.run(coro)
```

**问题**：每次 Celery 任务都创建和销毁一个事件循环。如果任务频繁触发，会有不必要的开销。

**修改方案**：
- 使用 `asgiref.sync_to_async` 或创建复用的事件循环
- 或将底层服务重构为纯同步版本（Celery worker 中不需要 async）

### 5.2 `asyncio.create_task()` 火后不管

**现状**：[react_agent.py:374](file:///d:/Projects/Python/Interview_Copilot/backend/app/agent_runtime/react_agent.py#L374)、[agent_executor.py:128](file:///d:/Projects/Python/Interview_Copilot/backend/app/agent/agent_executor.py#L128)

```python
asyncio.create_task(post_turn_maintenance_service.run(...))
```

**问题**：
- 任务异常会被静默吞掉（无 `add_done_callback`）
- 当 uvicorn shutdown 时，这些后台任务可能被强制中断
- `PostTurnMaintenanceService._locks` 使用普通 `dict`，非线程安全

**修改方案**：
- 添加 `TaskGroup` 或全局 background task set 跟踪
- 在 lifespan shutdown 阶段等待挂起任务完成
- 将 `_locks` 换成 `weakref.WeakValueDictionary` 防止内存泄漏

---

## 6. 🟡 前端工程（P2）

### 6.1 单文件巨型组件

**现状**：[App.vue](file:///d:/Projects/Python/Interview_Copilot/frontend/src/App.vue) — **483 行**，包含全部逻辑：
- 认证、会话管理、聊天、面试分析、记忆管理、模型配置

**修改方案**：
- 拆分为组件：`AuthView.vue`、`ChatPanel.vue`、`InterviewPanel.vue`、`MemoryPanel.vue`、`ModelPanel.vue`
- 添加 Vue Router 进行路由管理
- 使用 Pinia 进行状态管理（至少拆出 `authStore` 和 `sessionStore`）

### 6.2 前端缺失的功能

| 功能 | 当前状态 |
|------|---------|
| 会话搜索 | 有搜索框 UI，但 **未实现** 搜索逻辑 |
| 消息 Markdown 渲染 | 直接 `{{ message.content }}`，无格式化 |
| 会话删除 | 无 |
| 记忆详情/编辑/删除 | 仅展示前 5 条，无交互 |
| 知识库文件管理 | 无前端界面 |
| 错误重试 | 无 |
| Token 过期自动刷新 | 无 |

### 6.3 SSE 解析脆弱性

**现状**：[api.ts:101](file:///d:/Projects/Python/Interview_Copilot/frontend/src/api.ts#L101)
```typescript
const data = JSON.parse(line.slice(6));  // 无 try-catch！
```

**修改方案**：添加 `try-catch` 防止单个损坏事件导致整个流断开

---

## 7. 🟡 测试与可观测性（P2）

### 7.1 测试覆盖

**现状**：
- 有单元测试目录结构（`test_agent`、`test_api`、`test_core`、`test_db`、`test_models`、`test_rag`、`test_services`），但缺少端到端和集成测试
- conftest.py 使用 SQLite 内存数据库，**不测试 PostgreSQL 特性**
- 无 WebSocket/SSE 流式端点测试
- 无 RAG 精度回归测试（evaluation 目录有脚本但未集成 CI）

**修改方案**：
- 添加 `pytest-docker` 或 `testcontainers` 进行集成测试
- 将 `evaluation/run_rag_eval.py` 集成到 CI pipeline
- 添加 WebSocket 连接/消息测试

### 7.2 Telemetry 空壳

**现状**：[telemetry_service.py](file:///d:/Projects/Python/Interview_Copilot/backend/app/services/telemetry_service.py) 仅 1846 字节 — 基本是 log 打点

**修改方案**：
- 接入 OpenTelemetry SDK
- 添加 trace 和 span 上报到 Jaeger/Grafana
- 至少追踪：LLM 延迟、检索延迟、端到端请求延迟

---

## 8. 🟡 DevOps 与部署（P2）

### 8.1 缺少 Dockerfile

**现状**：docker-compose.yml 仅运行基础设施（DB, Redis, Milvus, MinIO），**API 和 Worker 在宿主机运行**

**修改方案**：
- 创建多阶段 Dockerfile（builder + runtime）
- 添加 `docker-compose.override.yml` 用于开发
- 生产使用 `gunicorn -k uvicorn.workers.UvicornWorker` 而非 `uvicorn --reload`

### 8.2 无 CI/CD

**修改方案**：
- 添加 GitHub Actions workflow：lint → test → build image → deploy
- 集成 `ruff` / `mypy` 代码质量检查

### 8.3 MinIO Bucket 公开访问

**现状**：[docker-compose.yml:57](file:///d:/Projects/Python/Interview_Copilot/docker-compose.yml#L57)
```shell
mc anonymous set public myminio/interview-copilot-bucket;
```

> [!CAUTION]
> 面试录音和简历文件所在的 S3 Bucket 被设置为 **完全公开**。任何人只要知道 URL 就能下载用户的敏感文件。

**修改方案**：
- 移除 `anonymous set public`
- 使用 presigned URL 进行授权访问（代码已支持）

---

## 代码级修改细节

### [MODIFY] [config.py](file:///d:/Projects/Python/Interview_Copilot/backend/app/core/config.py)
- 继承 `pydantic_settings.BaseSettings`
- 添加 `CORS_ORIGINS`、`EMBEDDING_DIM`
- 启动验证 SECRET_KEY

### [MODIFY] [main.py](file:///d:/Projects/Python/Interview_Copilot/backend/app/main.py)
- CORS origins 从配置读取
- lifespan 中添加 background task group 管理

### [MODIFY] [security.py](file:///d:/Projects/Python/Interview_Copilot/backend/app/core/security.py)
- 添加 Refresh Token 端点
- 迁移 datetime.utcnow → datetime.now(timezone.utc)

### [MODIFY] [database.py](file:///d:/Projects/Python/Interview_Copilot/backend/app/db/database.py)
- 添加连接池配置
- 可选引入 AsyncEngine

### [MODIFY] [retriever.py](file:///d:/Projects/Python/Interview_Copilot/backend/app/rag/retriever.py)
- BM25 索引缓存
- Milvus 连接复用
- 移除硬编码 EMBEDDING_DIM

### [MODIFY] [chat.py](file:///d:/Projects/Python/Interview_Copilot/backend/app/api/chat.py)
- WebSocket 端点使用 async session 或 run_in_threadpool
- 添加心跳和优雅断连

### [MODIFY] [react_agent.py](file:///d:/Projects/Python/Interview_Copilot/backend/app/agent_runtime/react_agent.py)
- 添加 streaming wrapper
- 修复 `asyncio.create_task` 异常捕获
- 修复 `_locks` 内存泄漏

### [MODIFY] [tasks.py](file:///d:/Projects/Python/Interview_Copilot/backend/app/worker/tasks.py)
- 优化 asyncio.run() 使用
- 添加重试策略（`autoretry_for`, `max_retries`）

### [MODIFY] [docker-compose.yml](file:///d:/Projects/Python/Interview_Copilot/docker-compose.yml)
- 移除 MinIO 公开访问
- 添加 API 和 Worker 容器定义

### [MODIFY] [App.vue](file:///d:/Projects/Python/Interview_Copilot/frontend/src/App.vue)
- 拆分为多组件架构

### [NEW] Dockerfile
### [NEW] .github/workflows/ci.yml
### [NEW] backend/app/db/async_database.py (可选)

---

## 执行路线图

### Phase 1: P0 安全与稳定性修复（1-2 天）
- [ ] 修复 CORS 配置
- [ ] Settings 迁移到 Pydantic BaseSettings
- [ ] 修复 JWT 安全缺陷
- [ ] 修复 MinIO 公开访问
- [ ] 数据库连接池配置

### Phase 2: P1 架构与性能优化（3-5 天）
- [ ] 统一 Agent 管线
- [ ] BM25 索引缓存 + Milvus 连接复用
- [ ] WebSocket/async 路径修复事件循环阻塞
- [ ] 后台任务生命周期管理
- [ ] Celery 任务重试策略

### Phase 3: P2 工程化与体验提升（3-5 天）
- [ ] 前端组件拆分 + 路由 + 状态管理
- [ ] 补充缺失的前端功能
- [ ] 创建 Dockerfile + CI/CD pipeline
- [ ] 集成 OpenTelemetry
- [ ] 端到端测试 + RAG 回归测试

---

## Open Questions

> [!IMPORTANT]
> 1. **Agent 管线统一**：你倾向于保留哪种模式？目前 WebSocket/SSE 用的是无 tool 调用的 `agent_executor`，而 `/api/v1/agent` 端点用的是有 tool 调用的 `react_agent`。统一成后者会改变对话流的行为。
>
> 2. **数据库异步迁移**：是否愿意迁移到 SQLAlchemy async engine？这需要较大的代码改动，但能从根本上解决事件循环阻塞。或者只用 `run_in_threadpool` 作为短期方案？
>
> 3. **前端重构深度**：是希望只做组件拆分和 bug 修复，还是愿意引入 Vue Router + Pinia 做完整的前端工程化？
>
> 4. **Phase 执行优先级**：你希望先从哪个 Phase 开始？我建议从 Phase 1 的安全修复开始。

---

## Verification Plan

### Automated Tests
```bash
# 现有测试确保不退化
pytest backend/tests -v

# 新增安全测试
pytest backend/tests/test_core/test_security.py -v

# 编译检查
python -m compileall -q backend/app
```

### Manual Verification
- CORS：用浏览器 DevTools 验证跨域请求行为
- JWT：Token 过期后验证 401 和刷新流程
- RAG 性能：对比修改前后的检索延迟（`evaluation/run_rag_eval.py`）
- WebSocket：使用 `scripts/test_ws.py` 验证多连接并发稳定性
