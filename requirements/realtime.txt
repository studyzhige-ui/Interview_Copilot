# Optional CPU media environment; never downloads model weights at runtime.
# Install with the API runtime, not in the GPU Qwen/CosyVoice environment.
aiortc==1.15.0
onnxruntime==1.23.2
transformers==4.57.6
